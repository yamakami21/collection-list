<div class="container">
    <div class="container-inner inner-default clearfix">
        <% @items.each do |item| %>
          <%= render partial: 'items/item', locals: { item: item } %>
        <% end %>
        <%= paginate(@items) %>
    </div>
</div>