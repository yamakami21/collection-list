<div class="item-box">
    <% if current_user.items.any? %>
            <div class="image">
                <div class="img-wrap">
                  <%= link_to item_path(item) do %>
                    <% if item.image? %>
                    
                  　　<span class="image" style="background-image: url('<%= item.image.thumb.url %>')"></span>
                  　
                  　<% else %>
                  　<% end %>
                  <% end %>
                </div>
            </div>
            <div class="detail">
                <span class="timestamp">
                    <%= item.created_at.in_time_zone("Asia/Tokyo").to_s(:date) %>
                </span>
                <% if logged_in? %>
                    <% if current_user == item.user %>
                    <% else %>
                        <span id="like_form-<%= item.id %>">
                            <%= render partial: 'likes/like_form',locals: { item: item } %>
                        </span>
                    <% end %>
                <% end %>
            </div>
    <% end %>
</div>
